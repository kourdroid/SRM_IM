## 2024-05-24 - Status Colors vs Accent Color
**Learning:** Open incidents must be Signal Red and resolved incidents Signal Green. Signal Orange is exclusively for reclamations. Electric Lime (accent) must never be used for status communication, only for primary actions/active tabs.
**Action:** Always map status directly to the `COLORS.signal*` semantic variables, never to `COLORS.accent`.
