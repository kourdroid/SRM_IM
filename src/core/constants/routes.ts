import type { Href } from 'expo-router';

// Expo Router's generated route types can lag new files until Metro regenerates them.
export const PENDING_APPROVAL_ROUTE = '/(auth)/pending-approval' as Href;
